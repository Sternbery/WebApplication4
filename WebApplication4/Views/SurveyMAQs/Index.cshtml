@model IEnumerable<WebApplication4.Models.SurveyMAQ>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ChoiceOrder)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SurveyQuestion.Text)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ChoiceOrder)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Text)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SurveyQuestion.Text)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MAQID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MAQID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MAQID })
        </td>
    </tr>
}

</table>
